import 'package:flutter/material.dart';

class Option3Page extends StatelessWidget {
  const Option3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Option 3')));
  }
}
